# Local development defaults are already handled by settings.py.
# Render values should be added in the Render Dashboard, not committed here.
SECRET_KEY=generate-a-new-secret-with-python-secrets
DEBUG=False
ALLOWED_HOSTS=queueless-hz3s.onrender.com
DATABASE_URL=paste-the-render-postgresql-internal-database-url-here
# Optional: only needed if you use Expo web or another browser client.
CORS_ALLOWED_ORIGINS=http://localhost:8081,https://your-web-client.example.com
